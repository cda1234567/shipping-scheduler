# 任務：算料工作區 階段二 —— 單一資料流（缺料計算只在後端算一份）

## 設計依據（先讀）
`.omc/plans/calc-modal-redesign-20260708.md` §5.1（preview API 形狀）、§6 階段 2（範圍/風險/驗證）、§3（三份重複計算的盤點）。目標：根治「後端有缺料前端沒列、數字對不上」這整類 bug——缺料計算只留後端一份，前端只渲染。

## 現況（行號以實際碼為準）
- 權威：app/services/shortage_rules.py；preview 地基：POST /api/schedule/main-write-preview（app/routers/schedule.py:825 附近，showWriteToMainModal 已在用）。
- 鏡像 1：static/modules/calculator.js:201-225（getRequiredMinStock / calculateShortageAmount / calculateCurrentOrderShortageAmount）。
- 鏡像 2：static/modules/schedule.js 的 getModalRequiredMinStock / calculateModalShortageAmount / calculateModalCurrentOrderShortageAmount（3186-3210 一帶，搬遷後行號有位移）。
- 前端 what-if 重算：refreshModalShortageCascadeForPart（schedule.js:3314 一帶）讀 DOM 重跑 running balance。
- 前端初算入口：showBatchMergeDraftModal / showShortageModal 用 buildRawModalShortageGroups→calculator.js calculate()。

## 實作要求
1. **後端 calc-preview**：把 main-write-preview 擴充（或新增 POST /api/schedule/calc-preview，沿用 BatchDispatchRequest，含 sample_order_ids 與新增 reset_stored: bool）：
   - reset_stored=true 時忽略已存的 order decisions/supplements（等同批次 Merge 重算語意），false 沿用。
   - 回傳按訂單 scope 分組結構（§5.1 的 Response 形狀：scopes[{order_id, batch_code, model, po_number, is_sample, shortages[…每列含 current_stock/prev_qty_cs/needed/shortage_amount/supplement_qty/moq/st_stock_qty/lookahead_*/decision/is_customer_material]}], blocking_count）。計算核心完全復用 build_dispatch_plan/preview_order_batches，不新寫公式。
2. **前端三個入口統一**：showBatchMergeDraftModal / showShortageModal 的缺料資料改打 calc-preview（batch merge 帶 reset_stored 依既有旗標），移除對 calculator.js calculate() 的初算依賴；showWriteToMainModal 照舊但改吃同一個 endpoint/結構。
3. **what-if 重算後端化**：使用者改補料/勾缺料/勾打樣時，debounce 250ms 打一次 calc-preview（帶當前輸入的 supplements/decisions/sample_order_ids），用回傳整組重繪該 scope；處理 in-flight 取消（後發請求蓋前發，AbortController）。refreshModalShortageCascadeForPart 的 DOM 重算邏輯移除。
4. **刪鏡像**：刪 calculator.js:201-225 與 schedule.js 的三個 Modal 鏡像函式；calculator.js 的 calculate() 若仍被其他地方引用（右側面板總覽等），保留該處用途但不再用於工作區（確認引用點再動，回報你怎麼處理）。
5. 使用者輸入期間的體感：打字當下輸入框的值不得被回寫蓋掉（重繪時保留 focus 中元素的值），這是歷史 bug 高發區（version_info 264/272/785）。

## 回歸網（必跑，都在驗收內）
- 全套 unittest（含 test_api 的 main-write-preview 案例）全綠。
- 新增測試：calc-preview 的 reset_stored true/false 差異、sample_order_ids 讓 EC 缺口歸零、scopes 分組含 batch_code。
- EC<100、EC-6 例外、PK<1、打樣歸零 各驗一顆（可加進新測試）。

## 版本
app/version_info.py：v2026.07.10.1（或依當天日期）、headline『缺料計算統一由後端計算，前後端數字不再不同步』、白話 changelog。

## 限制
- shortage_rules 本身一行不改；不動送出（batch-dispatch/update-and-commit-drafts）的寫入邏輯。
- 不做批次 Merge 按鈕合併（階段三）、不做 job 進度（階段四）。
- 最小 diff 原則下允許刪碼（刪鏡像與 DOM 重算是本階段目的）。

## 設計修正（2026-07-09 使用者拍板，優先於上文任何衝突處）
共用池料號（非 ORDER_SCOPED_PART_PREFIXES 的料，例如 EC-）**維持整批只補一次**：使用者明確要求一次扣 8-1~8-10 時，同一顆料不能要他補 10 次。因此 calc-preview 的 response 除了 per-scope 明細（顯示用），還要回一組 `shared_parts` 彙總：每個共用料一列（跨批總需求、跨批缺口、建議補料、目前補料、缺料決策），前端對共用料只渲染**一列**輸入（一個補料欄＋一個缺料勾選），送出沿用既有 flat-key 模型與後端分配邏輯。訂單專屬料（IC-STM 等）維持 per-order。這個「每料一列」結構同時從根上消滅跨列污染（不存在第二列可污染）。

## 驗收
1. C:/Users/Andy-STNB/AppData/Local/Programs/Python/Python313/python.exe -m unittest discover -s tests 全綠。
2. node --check schedule.js / calculator.js。
3. 回報：改動 檔案:行號、刪掉的函式清單、calc-preview request/response 實際形狀範例、calculator.js calculate() 其餘引用點的處置說明。
