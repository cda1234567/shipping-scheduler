# 任務：EC 補料被整批吃掉的止血修法（跨列 checkbox 汙染）

## 根因（已由追蹤報告確立，先讀 .omc/research/green-red-ec-bug-20260709.md）
使用者重現：8-1~8-9 重扣 → 綠色批次 Merge 顯示 EC 建議補到 100 → 紅色寫主檔 → EC 仍缺料。
因果鏈：
1. `shouldAutoShortageCheck`（static/modules/schedule.js:3244-3255）：某列 running 結存為負就**自動**把該列勾成「缺料」並 disable 補料輸入——計算中間狀態被當成使用者決策。
2. `_collectModalDecisions`（:3984-4008）與 `_collectModalSupplements`（:4010-4026）的 isShortage 判斷有一段 `Array.from(list.querySelectorAll(".shortage-mark")).some(...)` 跨列掃描：同料號**任一列**被勾，其餘列全部視為缺料 → 整批 EC 補料在送出瞬間被丟棄、decision 變 Shortage。
3. 後端忠實照 Shortage 不補料、照常扣帳 → 寫入後 EC <100。

## 止血改法（本次只做這個，結構性重做留給階段二）
1. **移除自動勾選**：`shouldAutoShortageCheck` 的「結存為負 → 自動勾缺料」行為改成**只顯示警示、不勾選、不 disable 輸入框**——在該列加醒目紅色警示字（例如「⚠ 結存為負」），checkbox 維持未勾、補料輸入框可編輯且帶建議值。只有使用者手動勾選才算缺料決策。已存 draft 裡明確為 Shortage 的決策（decision 已存 Shortage）維持既有勾選行為不變——只拿掉「因結存為負而自動勾」這一條。
2. **跨列 `.some()` 邏輯保留不動**（它讓 UI 與後端 flat-key 模型一致；拿掉會引入另一組行為變化，留給階段二用 per-order 結構正解）。
3. 前端加一道保險：送出（確認補料/確認補料並寫主檔）前，若偵測到「有補料輸入值 >0 的料號同時被跨列判定為 Shortage」，跳 confirm 白話警告「料號 X 有 N 列填了補料但因為某列勾了缺料，補料將不會送出，確定？」——讓這種衝突永遠不再無聲發生。

## 版本
app/version_info.py 升下一版號（v2026.07.09.x 接續），headline『修正批次寫入時 EC 補料被整批忽略的問題』，changelog 白話說明（結存為負不再自動勾缺料、衝突會先警告）。

## 驗收
1. node --check static/modules/schedule.js。
2. C:/Users/Andy-STNB/AppData/Local/Programs/Python/Python313/python.exe -m unittest discover -s tests 全綠（tests/test_frontend_assets.py 若有斷言受影響字串，同步更新不弱化）。
3. 寫一支 Playwright DOM 驗證腳本 tools/verify_ec_collection_fix.mjs（不對真實資料送出）：載入頁面 → 在 page context 構造假的 shortage list DOM（或用現有渲染函式）驗證「一列勾缺料時，_collectModalSupplements 對其他列同料號補料的收集結果」修正前後差異；至少驗證「無人勾選時補料會被收集」與「警告 confirm 的觸發條件」。做不到完整模擬就縮小驗證範圍並明說驗了什麼。
4. 回報：改動 檔案:行號、修正前後收集行為對照、警告文案。

## 限制
- 只改前端 static/ 與 version_info.py；後端一行不動。
- 不動 `.some()` 跨列判斷本身；不動 draft/order_supplements 既有資料。
- 注意 working tree 可能有另一個進行中的打樣改動——開工前先 git status 確認；若 schedule.js 有未 commit 改動，在其基礎上繼續改，不要 revert。
