# 任務：算料工作區 階段三 —— 批次 Merge 一顆＋兩勾選、打樣全路徑打通

## 設計依據（先讀）
`.omc/plans/calc-modal-redesign-20260708.md` §5.3、§5.4、§6 階段 3；merge 按鈕行為分析 `.omc/research/merge-buttons-20260708.md`（使用者已拍板選項 2）。
前置：階段一（工作區分頁）與階段二（calc-preview 單一資料流）已上線；打樣勾選已在兩個入口固定顯示（v2026.07.09.3）。

## 實作要求
### A. 批次 Merge 收成一顆＋兩個勾選（後端零改）
1. static/index.html 出貨排程工具列：移除「批次 Merge + 寫主檔」按鈕，保留一顆「批次 Merge」，旁邊兩個 checkbox：「重算補料」（預設**不勾**）、「同時寫主檔」（預設不勾）。checkbox 狀態記 localStorage 讓習慣延續。
2. schedule.js runBatchMergeWorkflow：reset_stored ← 「重算補料」勾選值；commitAfterModal ← 「同時寫主檔」勾選值（不再由按哪顆按鈕決定）。
3. 防呆：勾了「重算補料」送出前，若該批訂單偵測到已存的手填補料/決策（有既有 draft 資料即算），confirm「重算會清掉你手填的 N 筆補料，確定？」，取消則中止。
4. 文案：工作區工作列同步顯示目前模式（例如「模式：重算補料＋寫主檔」）。

### B. 打樣旗標全路徑
5. 副檔重建路徑：rebuild_merge_drafts / merge_drafts.py 的缺料計算吃 sample 旗標（勾了打樣的訂單，副檔裡 EC 料一樣不強制補到 100）。打樣旗標目前不落 DB——本階段允許把它存進 merge draft 的資料（draft 已有 JSON 結構，加一個 is_sample 欄位），這樣「存副檔→之後再開」時打樣狀態不會消失；重開工作區時從 draft 還原勾選狀態。
6. 發料單路徑：app/routers/bom.py 與 dispatch 發料單生成中用到 shortage_rules 的補料建議（bom.py:736 一帶），對打樣訂單同樣 ignore_ec_min（依據該單 draft 的 is_sample）。
7. 驗證一致性：同一張打樣單在 calc-preview、寫主檔、副檔、發料單四處的 EC 料都不被強制補到 100（設計文件 §6 階段3 的驗證要求）。

## 版本
app/version_info.py 照日期升版，headline『批次 Merge 合併成一顆＋兩個選項，打樣狀態全流程一致』，白話 changelog（含「重算補料會先警告」）。

## 限制
- 不動階段四範圍（job 進度）。
- shortage_rules 介面不再改（ignore_ec_min 已存在）。
- 最小 diff；移除的舊按鈕相關 dead code 要清乾淨。

## 驗收
1. C:/Users/Andy-STNB/AppData/Local/Programs/Python/Python313/python.exe -m unittest discover -s tests 全綠；新增測試：draft 帶 is_sample 時副檔重建 EC 缺口為 0、發料單建議補料為 0。
2. node --check schedule.js。
3. 回報：改動 檔案:行號、四種勾選組合各自的行為描述、打樣在四條路徑的生效點行號。
