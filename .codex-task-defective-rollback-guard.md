# 任務：退回沖掉不良品——警告清單 + 一鍵重放（設計文件的階段一＋階段二）

## 背景與設計依據
完整分析在 `.omc/research/defective-rollback-20260708.md`（先讀）。摘要：強制退回用發料當下的整份主檔備份覆蓋現行主檔，備份時點之後寫入的不良品/加工多打扣帳欄全被沖掉；守衛 ensure_dispatch_rollback_allowed 在 force 模式被跳過且只擋不補。defective_batches/defective_records 完整保留（料號、defective_qty、batch_id、imported_at/created_at、action_taken），扣帳是純 delta＋動態讀最右欄（deduct_defectives_from_main），天生可重放。

## 階段一：強制退回前把代價攤開
1. 後端 `_build_rollback_preview`（app/routers/schedule.py:92 附近）：force 模式時，用既有 `db.get_defective_batch_summaries_after(cutoff)`（database.py:3064）＋各批 records，在 preview response 加 `wiped_defectives` 欄位：[{batch_id, filename, imported_at, action_taken, items:[{part_number, defective_qty}]}]，cutoff = tail 第一筆 session 的 dispatched_at。
2. 前端 `handleRollbackDispatch`（static/modules/schedule.js:4467 附近）：force 確認框文案在現有警語後追加逐筆清單「⚠ 這次退回會沖掉以下不良品/加工多打扣帳，共 N 筆：\n1. EC-xxxx × 200（2026-07-08 匯入）…」。無 wiped_defectives 時文案照舊。

## 階段二：一鍵重放
3. 新 DB 查詢 `get_defective_records_after(cutoff)`（app/database.py，join defective_batches 取 imported_at/filename，只取 status 有效的 records，依 imported_at, created_at 排序）。
4. 新 service `replay_defectives_after(main_path, cutoff)`（建議放 app/services/defective_deduction.py）：records 依 batch_id 分組、依時間排序，逐批呼叫既有 `deduct_defectives_from_main`（不良品與加工多打各用原本的欄頭類型——參照 defectives.py 匯入與 overrun 路徑怎麼呼叫），全部完成後 `refresh_snapshot_from_main` 一次。回傳 {replayed_batches, replayed_records}。
5. 新 endpoint `POST /api/defectives/replay-after-rollback`（app/routers/defectives.py）：body {cutoff}；執行前先驗證 cutoff 之後沒有「刪除不良品批次」的攔截性 log（參照 inventory_restore_guard.py 的 _BATCH_DELETE_BLOCKING_LOG_ACTIONS 概念），有交錯就回 400 說明。動作前照慣例 backup_main_file。
6. 後端 rollback response（app/routers/schedule.py:1132 rollback_order）在 force 且有 wiped_defectives 時回傳 `replay_cutoff`。
7. 前端：退回成功且 result.replay_cutoff 存在時，toast 改 sticky 並附「補回 N 筆不良品」按鈕，按了 POST replay-after-rollback，成功後 showToast 顯示補回筆數並 refresh 相關畫面。

## 版本
8. app/version_info.py：APP_VERSION 升 v2026.07.09.1（若今天日期已是 07-09 之後照日期規則）、APP_RELEASED_AT 對應、APP_HEADLINE『退回不再默默沖掉不良品扣帳』，APP_CHANGELOG 加 entry（白話，沿用格式）：退回前列出會被影響的不良品清單；退回後一鍵補回。

## 限制
- 不改 rollback 的覆蓋還原邏輯本身（自動重放是之後的階段三，本次不做）。
- 重放一律走既有 deduct_defectives_from_main，不要另寫一套 Excel 寫入。
- 不動 tests/ 以外既有測試；**新增**測試：tests/ 加一個小案例驗 get_defective_records_after 的 cutoff 過濾與排序（用現有測試風格）。
- 最小 diff。不准動 .omc/、.codex-task-*.md、data_repo_backup*。

## 驗收
1. python -m unittest discover -s tests 全綠（本機已裝 openpyxl）。
2. 新增的 cutoff 測試涵蓋：早於 cutoff 不撈、晚於撈到、排序正確。
3. 回報：改動 檔案:行號 清單＋測試輸出摘要（Ran N / OK）。
