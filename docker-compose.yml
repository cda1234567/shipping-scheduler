name: dispatch-dev

services:
  dispatch-scheduler:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dispatch-scheduler
    restart: unless-stopped
    ports:
      - "8765:8765"
    volumes:
      - ./data:/app/data
