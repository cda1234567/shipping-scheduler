# 任務：已發料分頁快速勾選（全選 + Shift 範圍勾選）

## 背景
使用者在「已發料」分頁常要勾 10 幾筆訂單再按「生成發料單」，一筆一筆點太慢。要加快速勾選。

## 需求
1. **全選/取消全選**：在已發料分頁的工具列（「生成發料單」按鈕 `#btn-completed-gen-dispatch` 附近）加一個全選控制項（checkbox＋文字「全選」，樣式沿用現有 UI）。
   - 勾＝把目前畫面上所有已發料訂單的 checkbox 全部勾起（加進 `_completedCheckedIds`）；取消＝全部取消。
   - 若清單有資料夾分組/篩選，「全選」只作用於目前有渲染出來的訂單列。
   - 部分勾選時全選框顯示 indeterminate 狀態（可選，做得到就做）。
2. **Shift 範圍勾選**：按住 Shift 點某筆的 checkbox 時，把「上一次點擊的那筆」到「這次點的這筆」之間（依畫面顯示順序）的所有訂單，一次設成這次點擊後的狀態（勾或取消）。記住 last-clicked 索引即可，跨資料夾分組也照顯示順序處理。

## 相關程式碼位置（已查好）
- `static/modules/schedule.js`
  - 勾選狀態集合：`_completedCheckedIds`（Set，約 24 行附近宣告區）
  - 單筆 checkbox 渲染與 change 監聽：約 4105、4122–4130 行，class `completed-order-check`
  - 勾選數量/按鈕啟用邏輯：約 4014 行 `getCompletedCheckedOrderIds().length`
  - 分頁渲染：`renderCompletedTab()`
- 工具列 HTML 若在 `static/index.html`（搜 `btn-completed-gen-dispatch`，157 行附近）就改那裡；若工具列是 JS 動態產生就改 JS。

## 限制
- 只改「已發料」分頁的勾選行為；不動出貨排程分頁、不動後端。
- **不准動 `app/routers/dispatch.py`**（那裡有另一個未 commit 的改動，保持原樣）。
- 最小 diff，沿用現有命名與樣式，不新增框架/依賴。
- 全選或範圍勾選後，勾選數量顯示與「生成發料單」按鈕的啟用狀態要正確更新（跟單筆勾選走同一條更新路徑）。

## 驗收
- 點全選 → 畫面上每筆 checkbox 都勾起、按鈕啟用、數量正確；再點 → 全部取消。
- 勾第 2 筆後 Shift+點第 9 筆 → 第 2~9 筆全勾。Shift+點已勾的範圍端點做取消 → 該範圍全取消。
- 單筆勾選行為不變。
- 說明你改了哪些行（檔案:行號），並自行檢查 JS 語法（node --check 或等效方式）。
