# 任務：已發料資料夾支援巢狀（資料夾裡再放資料夾）

## 背景
「已發料」分頁目前的資料夾是扁平一層（訂單的 `folder` 字串欄位）。使用者要求資料夾可以再放進資料夾（例如「2026」裡面放「6月」「7月」）。

## 方案（已定，照做）
用路徑字串表示巢狀：`folder` 值允許 `父/子` 格式（例如 `2026/6月`），分隔符號固定 `/`。**後端不改**——`folder` 本來就是任意字串，訂單移動 API、`/api/schedule/completed` 回傳皆不受影響。全部改動在前端。

## 相關程式碼位置（已查好，都在 static/）
- `static/modules/schedule.js`
  - `renderCompletedTab()`：3974 行起，目前按 folder 扁平分組、每組依 code 排序後用 `buildFolderSection()` 渲染
  - `buildFolderSection(folderName, rows, allFolders)`：4061 行起，含標題列（collapse 箭頭、刪除 ✕）與訂單卡片
  - 訂單卡片上的資料夾下拉：4087 行附近 `folderOptions`
  - `handleCreateFolder()`：4237 行起（`#new-folder-name` 輸入框，前端先記住，訂單移入才真的存在）
  - collapse 狀態：localStorage `completed-folder-collapsed-state`，key 是 folder 名稱（沿用，key 就是完整路徑）
  - 注意：4030 行起的 `updateCompletedToolbarState()`、全選與 Shift 範圍勾選（`getRenderedCompletedCheckControls()`、`_completedLastCheckedId`）是剛加的功能，改渲染結構後必須保持能用（它們依 DOM 顯示順序運作）
- `static/index.html`：已發料工具列在 157 行附近
- `static/style.css`：`.completed-folder-*` 樣式在 2230 行附近

## 需求細節
1. **渲染成樹**：把所有 folder 路徑（`_completedFolders` ∪ 實際出現在訂單上的 folder 值）建成樹。子資料夾的 section 巢狀渲染在父資料夾 section 的 body 裡、加縮排。父資料夾即使沒有直屬訂單、只要有子孫資料夾就要渲染（顯示的數量 = 含子孫的訂單總數，直屬訂單數可加括號另示或不顯示，從簡）。收合父資料夾時整個 body（含子資料夾）隱藏——沿用現有 collapse 機制即可。
2. **建立子資料夾**：
   - `#new-folder-name` 輸入 `A/B` 直接建立巢狀資料夾（沿用 handleCreateFolder 的前端記住機制，校驗：不可空段、不可重複、深度上限 3 層，超過用 showToast 提示）。
   - 每個資料夾標題列加一個小按鈕「＋」（title=新增子資料夾）：點了把 `#new-folder-name` 預填為 `該資料夾路徑/` 並 focus，讓使用者接著打名字。
3. **訂單的資料夾下拉**：選項列出所有路徑，依樹狀順序排列並用全形空白縮排顯示子層（value 一律是完整路徑）。
4. **刪除資料夾 ✕**：若該資料夾還有子資料夾，showToast「請先刪除或清空子資料夾」並中止；否則沿用現有行為（直屬訂單移回未歸檔）。
5. **未歸檔** section 維持在最外層最後，不參與巢狀。
6. 全選、Shift 範圍勾選、勾選數量、生成發料單在新結構下行為不變（範圍依畫面顯示順序，跨資料夾照舊）。

## 限制
- **不准動後端任何檔案**（尤其 `app/routers/dispatch.py` 有未 commit 改動）。
- 只改 `static/index.html`、`static/style.css`、`static/modules/schedule.js`。
- 最小 diff、沿用現有樣式語彙（不引入新框架）。不做拖拉移動、不做資料夾改名（未要求）。

## 驗收
- 建立 `2026`、`2026/6月`，把訂單移入 `2026/6月`：重新渲染後 `2026` 內縮排顯示 `6月`，數量統計正確；收合 `2026` 後整組隱藏。
- 下拉選單能直接把訂單移到 `2026/6月`。
- 刪除有子資料夾的父資料夾被擋下並提示。
- 全選/Shift 範圍勾選在巢狀結構下仍正確。
- `node --check static/modules/schedule.js` 通過。
- 回報改動 檔案:行號 清單。
