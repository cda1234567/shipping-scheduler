# 任務：發料（扣帳）時可勾「打樣」，該訂單忽略 EC 料結存 <100 需補料規則

## 背景
EC- 開頭料號有「結存不得低於 100」規則：發料後結存 <100 就會要求補料補到 100（EC-6 例外）。打樣訂單用量極小，使用者不想被迫補到 100。需求：在發料確認視窗對單一訂單勾「打樣」，該訂單這次扣帳就忽略這條 <100 規則（缺口以 0 計）。其他訂單、其他頁面的缺料/補料計算不受影響。旗標**不落資料庫**，只作用於這一次發料請求。

## 現況（已查證，行號為目前 HEAD 附近，實作時以實際碼為準）
- 規則核心：`app/services/shortage_rules.py:4` `EC_MIN_ENDING_STOCK = 100.0`；`get_min_ending_stock()`（:44-55）對 `EC-` 回 100；`calculate_shortage_amount()`（:58-60）算 `max(0, required_min - ending_stock)`。
- 後端兩條發料路：
  - 單筆：POST `/api/schedule/orders/{id}/dispatch` → `app/routers/schedule.py:800` → `_resolve_single_order_dispatch_plan`（:403）
  - 批次：POST `/api/schedule/batch-dispatch` → `app/routers/schedule.py:829` → `_resolve_batch_dispatch_plan`（:429）
  - 兩條都收斂到 `DispatchContext`（`app/services/dispatch_pipeline.py:28`）→ `build_dispatch_plan`（:369）→ `preview_order_batches` / 寫主檔（`app/services/merge_to_main.py`，<100 套用點在 :331、:340 呼叫 shortage 計算）。
- Request models：`app/models.py:134 BatchDispatchRequest`、`:143 DecisionRequest`（目前無 sample 欄位）。
- 前端發料確認視窗：`showWriteToMainModal`（`static/modules/schedule.js:2957`），內容按訂單 scope 分組（`buildShortageScopeList`，:2984 附近），送出 handler `handleModalWriteMain`（:3953）→ 批次 endpoint（:3975）。前端缺料計算鏡像：`static/modules/calculator.js:203-204`（EC- 回 100）。

## 實作要求
### 後端
1. `BatchDispatchRequest` 加 `sample_order_ids: list[int] = Field(default_factory=list)`；`DecisionRequest`（單筆）加 `is_sample: bool = False`。
2. 旗標經 `_resolve_batch_dispatch_plan` / `_resolve_single_order_dispatch_plan` 進 `DispatchContext`（新欄位，例如 `sample_order_ids: set[int]`），在 merge_to_main 的 per-order 計算處生效：該 order 計算 EC 料缺口時 required_min 以 0 計。
3. 實作方式：`shortage_rules.get_min_ending_stock()` / `calculate_shortage_amount()` 加可選參數（例如 `ignore_ec_min: bool = False`，True 時 EC- 回 0），呼叫端只在「該訂單被標記打樣」時傳 True。**不要**動全域常數、不要影響其他呼叫點（calculator.py、merge_drafts.py、order_supplements.py、bom.py 一律維持原行為）。
4. preview（發料預覽）與實際寫主檔兩處都要吃到旗標，行為一致。

### 前端
5. 發料確認視窗每個訂單 scope 的群組表頭加一個 checkbox「打樣（此單 EC 料不強制補到 100）」，預設不勾。
6. 勾/取消時即時重算**該 scope** 顯示的缺料/補料列：勾了之後 EC 料以 required_min=0 重算（calculator.js 的鏡像函式加同樣的可選參數或等效處理），原本因 <100 出現的補料列應消失或缺口變 0；取消恢復。
7. `handleModalWriteMain` 送出時收集勾了打樣的 scope 對應 order_ids，放進 `sample_order_ids` 一併 POST。
8. 單筆 dispatch 的 confirm() 路線這次**不加 UI**（維持現狀），但後端 `is_sample` 欄位照加（供未來用）。

## 限制
- 不動資料庫 schema、不加 settings。
- 最小 diff；不要重構既有 shortage 計算的呼叫結構。
- 不准動 `.codex-task-*.md`、`.omc/`。
- 前端後端參數命名一致（sample_order_ids / is_sample）。

## 驗收
1. 寫一段獨立可跑的驗證（不起 server）：直接呼叫 shortage_rules 兩函式，EC- 料在 ignore 時回 0/缺口 0、不 ignore 時回 100/缺口=100-結存；EC-6、PK-、一般料不受 ignore 影響（本來就 0/1/0）。跑出結果給我看。
2. `node --check static/modules/schedule.js`、`python -c "import ast;ast.parse(...)"`（改到的每個 .py）通過。
3. 回報改動 檔案:行號 清單，以及旗標從 UI → request → DispatchContext → merge_to_main → shortage_rules 每一站的 檔案:行號（讓我能抽查整條鏈）。
