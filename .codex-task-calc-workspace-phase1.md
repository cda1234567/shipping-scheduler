# 任務：算料工作區 階段一 —— 去彈窗＋誠實進度（設計文件 §6 階段 1）

## 設計依據（先讀）
`.omc/plans/calc-modal-redesign-20260708.md`（重點：§5.2 嵌入式界面、§5.5 進度、§6 階段 1 的範圍與明確不做的事）。本階段**只搬容器與進度呈現，一行計算邏輯都不准動**。

## 需求
1. **新分頁**：static/index.html 頂部 tab 列（103-114 行附近）加 `<button class="tab-btn" data-tab="calc-workspace">算料工作區</button>` 與對應 tab-content 容器 `#tab-calc-workspace`，位置放在「已發料」之後。平常沒有進行中工作時顯示空狀態提示（「從出貨排程按『批次 Merge』或『生成發料單』開始」）。
2. **render 目標搬遷**：showBatchMergeDraftModal / showWriteToMainModal / showShortageModal / showDraftModal（schedule.js:2502-3030 一帶）原本 render 進 #shortage-modal 的內容，改 render 進 #tab-calc-workspace 內的容器，並自動切換到算料工作區分頁（沿用既有 .tab-btn 切換機制）。原 #shortage-modal overlay 的 DOM 與 CSS 保留骨架但不再被這四條路徑使用（之後階段再清）。
3. **移除彈窗病灶**：
   - openBatchMergeDraftModalStable（schedule.js:6049 附近）的「render 兩次 retry」刪除——tab 內容渲染不需要。
   - setModalDownloadProgress（schedule.js:3946-3994 附近）不再 disable 關閉/取消類按鈕。
   - 原「關閉 modal」動作改為「離開工作區」：切回出貨排程分頁；工作區內容保留（使用者切回來還在），原 closeShortageModal 的狀態清理只在明確按「清除工作區」時執行（工作列加這顆小按鈕）。
4. **誠實進度**：startModalProgressAnimation(92,…)（schedule.js:2807、4018 附近）的假百分比動畫改為不定量 spinner＋階段文字（例如「正在寫入主檔…（10 筆訂單約需 1 分鐘，可先切到其他分頁）」），完成/失敗才收尾。失敗統一：進度區轉紅、列出後端回傳的 failures/negative_shortages，不自動消失。
5. **送出不鎖頁**：寫主檔期間使用者可切任意分頁（本來 overlay 擋住的行為自然消失，確認沒有殘留的 pointer-events/body lock）。送出完成時若使用者不在工作區分頁，用既有 showToast 通知結果。
6. 工作列（sticky）：搬遷後在工作區頂部放現有 footer 按鈕（存副檔／寫入主檔／下載等，依原 mode 顯示）＋進度區＋「清除工作區」。樣式沿用現有 UI 語彙（style.css .completed-* / .modal-* 風格）。

## 不准做（階段紀律）
- 不動任何缺料計算、補料收集、送出 payload 邏輯（calculator.js、refreshModalShortageCascade*、collectModal* 等原樣沿用）。
- 不動後端任何檔案（version_info.py 除外）。
- 不做批次 Merge 按鈕合併（那是階段三）、不做 preview API（階段二）。
- 打樣 checkbox 等現有 modal 內容原樣跟著搬，不改行為。

## 版本
app/version_info.py：v2026.07.09.2、headline『算料改為工作區分頁，不再彈窗鎖畫面』、changelog 白話 entry（工作區分頁、可自由切換分頁、進度改階段文字、失敗會列明細不再卡 92%）。

## 驗收
1. node --check static/modules/schedule.js 通過；python ast 檢查 version_info.py。
2. C:/Users/Andy-STNB/AppData/Local/Programs/Python/Python313/python.exe -m unittest discover -s tests 全綠（tests/test_frontend_assets.py 若有斷言 modal 相關字串，照搬遷後的實際碼更新，但不得弱化測試意圖）。
3. 回報：改動 檔案:行號 清單＋「四條入口各自搬去哪」的對照。
