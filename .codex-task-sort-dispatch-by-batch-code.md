# 任務：發料單生成順序改為依批次欄（X-X）排序

## 背景
使用者在「已發料」分頁勾選多筆訂單按「生成發料單」時，目前產出的發料單順序＝勾選順序（前端 `_completedCheckedIds` Set 的插入順序原樣送到後端）。使用者要求：產出順序一律照訂單的批次代碼（`code` 欄位，格式 `數字-數字`，例如 `23-1`、`23-2`、`24-1`）排序，不要照勾選順序。

## 修改位置
`app/routers/dispatch.py` 的 `_generate_dispatch_response()`（約 430 行起）。
在 `orders = _get_selected_orders(requested_ids)`（約 439 行）取得訂單列表之後、後續使用 `orders` 之前，把 `orders` 就地重排。

## 排序規則
- 對每筆 order 取 `order.get("code")`，若符合正規式 `^\d+-\d+$`：sort key = `(0, 主號整數, 次號整數)`。
- 不符合（空值或其他格式）：sort key = `(1, 原始位置索引)`——排在所有有效批次碼之後，維持原相對順序。
- 用穩定排序（Python `sorted` 即可）。

## 限制
- 只改 `app/routers/dispatch.py` 這一處，最小 diff。
- 不改前端、不改其他 endpoint、不動排版與無關程式碼。
- 不新增設定選項。

## 驗收
- 傳入 order_ids 順序打亂（例如 code 為 24-2、23-1、23-2 的三筆），產出的 groups 順序為 23-1、23-2、24-2。
- code 為空的訂單排在最後，且彼此維持傳入順序。
- 寫一小段可獨立執行的驗證（直接對排序函式/邏輯做 in-memory 測試即可，不必起 server），跑給我看結果。
