# 任務：算料工作區 階段四 —— 寫主檔真進度（已寫入 N/總數）

## 設計依據（先讀）
`.omc/plans/calc-modal-redesign-20260708.md` §5.5 長期方案、§6 階段 4。前置：階段一~三已上線。

## 實作要求
1. 後端 update-and-commit-drafts（app/routers/schedule.py:934 一帶）改成 job 模式：
   - POST 建立 commit job → 立即回 {job_id}；實際寫入在背景 thread 執行（run_in_threadpool / threading，注意與現有單發路徑的鎖與 backup 語意一致，all-or-nothing 不變：進度只是回報，失敗仍整批 rollback）。
   - GET /api/schedule/commit-job/{job_id} 回 {status: running|done|failed, written: N, total: M, failures, negative_shortages, result}。進度粒度用現有 for-loop 逐筆更新（design doc：後端本就逐筆處理，粒度天然存在）。
   - job 狀態存記憶體 dict 即可（單機單進程），保留最近 10 筆，附建立時間。
2. batch-dispatch（發料單流程寫主檔）同樣模式或維持單發——依實測耗時決定：先量 10 筆訂單耗時，若 <10 秒維持單發並在回報說明；update-and-commit（實測 ~69 秒）必改。
3. 前端：送出後輪詢 job（1.5s 間隔），工作列進度顯示「已寫入 N/M 筆」；完成/失敗沿用階段一的收尾（成功 toast／紅色明細）。輪詢中斷（使用者關瀏覽器再開）：重新進工作區時若有 running job 自動接上繼續顯示。
4. 逃生：job 卡住（超過 10 分鐘 running）前端顯示警告並提供「查看後端紀錄」提示；不做取消功能（寫 Excel 中途取消風險大於收益）。

## 版本
app/version_info.py 照日期升版，headline『寫主檔顯示真實進度』，白話 changelog。

## 限制
- all-or-nothing 語意、backup 時機、寫入順序一律不變；只加進度回報層。
- 不動 shortage 計算與 UI 其他部分。

## 驗收
1. 全套 unittest 全綠；新增測試：job 建立→輪詢→done 的狀態機（可用小資料模擬）。
2. node --check schedule.js。
3. 回報：改動 檔案:行號、job 狀態機說明、batch-dispatch 是否改造的實測依據。
